<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.app.vipin.taskcreator.ScreenSlidePageFragment">

    <TextView
        style="@style/pager_task_title"
        android:textAlignment="center"
        android:text="HELLO"
        android:id="@+id/pager_task_title" />

    <TextView
        style="@style/pager_task_detail"
        android:text="Medium Text"
        android:id="@+id/pager_task_detail"
        android:layout_below="@+id/pager_task_title"/>
</RelativeLayout>